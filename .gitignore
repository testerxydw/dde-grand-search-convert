build/
*.o
*.user
build.dist/
dist/

# Debian 打包构建产物（dpkg-buildpackage 生成，非源码）
obj-*/
debian/.debhelper/
debian/*.debhelper.log
debian/*.substvars
deblast/debian/debhelper-build-stamp
debian/files
# 临时安装树（gitignore 不支持行尾 # 注释，须放行首）
debian/convert-search-plugin/
debian/tmp/
*.buildinfo
*.changes
*.deb

