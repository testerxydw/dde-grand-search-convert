build/
*.o
*.user
build.dist/
dist/

# Debian 打包构建产物（dpkg-buildpackage 生成，非源码）
obj-*/
debian/.debhelper/
debian/*.debhelper.log
debian/*.substvars
deblast/debian/debhelper-build-stamp
debian/files
debian/convert-search-plugin/  # 临时安装树
*.buildinfo
*.changes
*.deb

